import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  LoginCardOuter: {
    display: 'flex',
    flexDirection: 'column',
    width: '100%',
    height: '100%',
  },

  LoginCardForm: {
    width: '100%',
    height: '75%',
  },

  LoginCardGreet: {
    width: '100%',
  },
});
