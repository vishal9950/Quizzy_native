import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  RadioButtonBody: {
    fontWeight: 'bold',
    zIndex: 1,
    flexDirection: 'row',
    alignItems: 'center',
  },

  RadioButtonLabel: {
    fontSize: 15,
  },
});

